// Re-export from source — no manual copies to keep in sync
export {
  formatTime,
  getDayOffset,
  parseHafasTimeToMin,
  calcDelay,
  parseCoord,
  parseZoom,
  getDelayClass,
  getDelayText,
  decodePolyline,
  extractHafasMessages,
} from '../../static/state.js';
